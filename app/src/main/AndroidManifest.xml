<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.team5_luss">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature android:name="android.hardware.type.watch" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RetrievalForm"></activity>
        <activity android:name=".CollectionListActivity"></activity>
        <activity android:name=".ItemDetails"></activity>
        <activity android:name=".AdjustVoucherDetails"></activity>
        <activity android:name=".AdjustVoucherListing"></activity>
        <activity android:name=".AdjustVoucherCreate"></activity>
        <activity android:name=".DisbursementByRequestActivity"></activity>
        <activity android:name=".DisbursementActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity"/>
<!--        <activity android:name=".CollectionListActivity"/>-->

    </application>

</manifest>